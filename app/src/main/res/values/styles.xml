<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Tomato</item>
        <item name="colorPrimaryDark">@color/Tomato</item>
        <item name="colorAccent">@color/Tomato</item>
    </style>


    <style name="AppButton" parent="Base.Widget.AppCompat.Button">
        <!--<item name="android:background">@drawable/ic_launcher_foreground</item>-->
        <item name="android:background">@drawable/ic_circular_button</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
